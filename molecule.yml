---
dependency:
  name: galaxy
driver:
  name: docker
docker:
  containers:
    - name: ansible-role-cvmfs
      image: centos
      image_version: latest
      ansible_groups:
        - group1
      privileged: True
      dockerfile: tests/devel-centos7/Dockerfile
#verifier:
#  name: testinfra

ansible:
  playbook: tests/test.yml
  diff: True
  verbose: True
  ansiblecfg_defaults:
    roles_path: ../../
    ansible_python_interpreter: /usr/bin/python
  limit: False
